{
  "name": "employee-project-dashboard",
  "version": "1.0.0",
  "description": "Employee and project dashboard with monthly snapshots.",
  "type": "module",
  "scripts": {
    "lint": "node --check app.js && node --check tests/calculations.test.js",
    "test": "node tests/calculations.test.js",
    "start": "python3 -m http.server 4173"
  },
  "devDependencies": {}
}
